import Rx from 'rxjs';
import graficarPunto from './graficarPunto'

const Observable = Rx.Observable;

/**
 * Crear un observable para conectarnos a un servidor local
 * a través de websocket [localhost:8081].
 * operador: webSocket
 */


/**
 * Procesar los datos provenientes del servidor
 * para usar la función `graficarPunto`.
 */


/**
 * Agregar soporte para reconexión al websocket.
 * Clave: retryWhen.
 */